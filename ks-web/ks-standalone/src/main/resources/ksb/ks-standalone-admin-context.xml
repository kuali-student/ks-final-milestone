<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
                           http://www.springframework.org/schema/tx
                           http://www.springframework.org/schema/tx/spring-tx-2.0.xsd
                           http://www.springframework.org/schema/aop
                           http://www.springframework.org/schema/aop/spring-aop-2.0.xsd">

	<!-- create an empty bean for disabling beans we don't want -->
	<bean id="nullBean" class="java.lang.Object" />

	<!-- turn off the MessageService jpa impl by killing its beans -->
	<!--<alias alias="messageEntityManagerFactory" name="nullBean" />
	<alias alias="messageEntityManager" name="nullBean" />
	<alias alias="messageDao" name="nullBean" />
	<alias alias="ks.exp.messageService" name="nullBean" />-->
	<bean id="messageEntityManagerFactory" parent="nullBean" />
	<bean id="messageEntityManager" parent="nullBean" />
	<bean id="messageDao" parent="nullBean" />
	<bean id="ks.exp.messageService" parent="nullBean" />

	<!-- the ksb exporter for the ks-core message service does not have an id so override the jpa impl with the ks-admin ojb impl -->
	<!--<alias alias="messageServiceImpl" name="ks.messageService" />-->
	<bean id="messageServiceImpl" parent="ks.messageService"/>
	
	<!-- the gwt application code is defaulting to the mock service impl, so override it as well -->
	<!--<alias alias="ks.messageServiceMock" name="ks.messageService" />-->
	<bean id="ks.messageServiceMock" parent="ks.messageService"/>
	
	<bean id="ks.messageService" class="org.kuali.rice.ksb.messaging.KSBClientProxyFactoryBean">
		<property name="serviceEndpointInterface" value="org.kuali.student.core.messages.service.MessageService" />
		<property name="serviceQName" value="{http://student.kuali.org/wsdl/messages}MessageService" />
	</bean>
</beans>
