CREATE TABLE LEARN_UNIT_S
(
	id bigint(19) not null auto_increment, primary key (id) 
) ENGINE MyISAM
/
ALTER TABLE LEARN_UNIT_S auto_increment = 1000
/
CREATE TABLE LEARN_UNIT_T
(
      LU_ID DECIMAL(8)
        , STATE VARCHAR(100) NOT NULL
        , TITLE VARCHAR(100) NOT NULL
        , VER_NBR DECIMAL(8) default 0
        , OBJ_ID VARCHAR(36) NOT NULL
    
    , CONSTRAINT LEARN_UNIT_TP1 PRIMARY KEY(LU_ID)
    , CONSTRAINT LEARN_UNIT_TC0 UNIQUE (OBJ_ID)

) ENGINE InnoDB CHARACTER SET utf8 COLLATE utf8_bin
/

CREATE TABLE KS_DEPT_T
(
        DEPT_CD VARCHAR(4) NOT NULL
        , DEPT_DESC VARCHAR(100) NOT NULL
        , VER_NBR DECIMAL(8) default 0
        , OBJ_ID VARCHAR(36) NOT NULL
    
    , CONSTRAINT KS_DEPT_TP1 PRIMARY KEY(DEPT_CD)
    , CONSTRAINT KS_DEPT_TC0 UNIQUE (OBJ_ID)

) ENGINE InnoDB CHARACTER SET utf8 COLLATE utf8_bin
/