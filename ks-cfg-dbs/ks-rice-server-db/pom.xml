<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.kuali.student.db</groupId>
    <artifactId>ks-cfg-dbs</artifactId>
    <version>1.0.1-SNAPSHOT</version>
  </parent>
  <artifactId>ks-rice-server-db</artifactId>
  <name>KS Rice Server Database</name>
  <description>Kuali Student Rice Server Database</description>
  <properties>
    <ks.rice.server.url>jdbc:oracle:thin:@localhost:1521:XE</ks.rice.server.url>
    <ks.rice.server.schema>KSRICE</ks.rice.server.schema>
    <ks.rice.server.username>KSRICE</ks.rice.server.username>
    <ks.rice.server.password>KSRICE</ks.rice.server.password>
  </properties>
  <build>
    <resources>
      <resource>
        <directory>src/main/resources/impex</directory>
      </resource>
      <resource>
        <directory>${project.build.directory}/generated-sql/impex</directory>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.kuali.impex</groupId>
        <artifactId>maven-impex-plugin</artifactId>
        <configuration>
          <targetDatabase>${ks.db.type}</targetDatabase>
          <driver>${ks.db.driver}</driver>
          <url>${ks.rice.server.url}</url>
          <schema>${ks.rice.server.schema}</schema>
          <username>${ks.rice.server.username}</username>
          <password>${ks.rice.server.password}</password>
        </configuration>
<!-- 
        <executions>
          <execution>
            <id>generate-data-dtd</id>
            <goals>
              <goal>datadtd</goal>
            </goals>
          </execution>
          <execution>
            <id>generate-oracle-sql</id>
            <configuration>
              <targetDatabase>oracle</targetDatabase>
            </configuration>
            <goals>
              <goal>schemasql</goal>
              <goal>datasql</goal>
            </goals>
          </execution>
          <execution>
            <id>generate-mysql-sql</id>
            <configuration>
              <targetDatabase>mysql</targetDatabase>
            </configuration>
            <goals>
              <goal>schemasql</goal>
              <goal>datasql</goal>
            </goals>
          </execution>
        </executions>
 -->
      </plugin>
    </plugins>
  </build>
</project>