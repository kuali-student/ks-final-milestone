<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.kuali.student.db</groupId>
        <artifactId>ks-cfg-dbs</artifactId>
        <version>2.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>ks-bundled-db</artifactId>
    <name>KS Bundled Impex</name>
    <description>Kuali Student Bundled Impex</description>
    <properties>
        <jdbc.username>KSBUNDLED</jdbc.username>
        <jdbc.password>KSBUNDLED</jdbc.password>
    </properties>
    <profiles>
        <profile>
            <id>impex-export-xml</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.kuali.maven.plugins</groupId>
                        <artifactId>maven-impex-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>impex-generate-sql</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.kuali.maven.plugins</groupId>
                        <artifactId>mvn-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>ks-db</id>
            <properties>
                <!-- Schemas -->
                <sql.meta.base>classpath:META-INF/sql/${db.vendor}/${project.artifactId}</sql.meta.base>

                <!-- SQL resources -->
                <sql.schema.1.list>${sql.meta.base}-schema.resources</sql.schema.1.list>

                <sql.data.1.list>${sql.meta.base}-data.resources</sql.data.1.list>

                <sql.constraints.1.list>${sql.meta.base}-constraints.resources</sql.constraints.1.list>

                <sql.schema.concurrent>sql.schema.1.list</sql.schema.concurrent>
                <sql.data.concurrent>sql.data.1.list</sql.data.concurrent>
                <sql.constraints.concurrent>sql.constraints.1.list</sql.constraints.concurrent>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.kuali.maven.plugins</groupId>
                        <artifactId>spring-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>meta-inf</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.kuali.maven.plugins</groupId>
                        <artifactId>metainf-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
