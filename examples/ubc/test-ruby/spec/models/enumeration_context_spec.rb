require 'spec_helper'

describe EnumerationContext do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    EnumerationContext.create!(@valid_attributes)
  end
end
